version: 2
updates:
  # Terraform updates disabled due to Dependabot bug with hashicorp/kubernetes provider
  # See: https://github.com/dependabot/dependabot-core/issues/XXXX
  # - package-ecosystem: "terraform"
  #   directory: "/terraform"
  #   schedule:
  #     interval: "weekly"
  # GitHub Actions updates disabled - no workflows in repository
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  - package-ecosystem: "docker"
    directory: "/kubernetes/production"
    schedule:
      interval: "weekly"
  - package-ecosystem: "docker"
    directory: "/kubernetes/testing"
    schedule:
      interval: "weekly"
